# 长视频创作协作入口（Claude）

本目录下所有项目共用此规则，开始任何工作前先读：

1. 本目录 `COLLABORATION.md` — 双引擎共享底线规则（10s 上限 / 8-A / subject 硬规则 / 分工路由）
2. **项目子目录**的 `HANDOFF.md` — 该项目的进度交接档

新开项目时，在项目子目录自动生成 `HANDOFF.md`（模板见 `_协作模板\HANDOFF.md`）。
完成任一阶段后，**必须更新项目子目录的 `HANDOFF.md`**（当前阶段、产物清单、校验状态、给下一棒的注意事项）。
